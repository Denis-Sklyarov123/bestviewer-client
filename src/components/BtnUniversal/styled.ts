import { Button, ButtonProps } from "react-bootstrap";
import styled from "styled-components";

export const StyledBtnUniversal = styled(Button)<ButtonProps>`
  background-color: #06c149;
  border-radius: 10vh;
  min-width: 40vh;
  color: white;
`;
